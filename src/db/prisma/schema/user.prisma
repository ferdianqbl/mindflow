model User {
  id        String     @id // Maps to Supabase auth.users.id
  email     String     @unique
  password  String
  createdAt DateTime   @default(now())
  logs      FocusLog[]
  plans     TaskPlan[]

  @@map("users")
}